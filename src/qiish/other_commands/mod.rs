//! Other builtin rust-based commands for Qiish
// Really, this is just a directory to them.

// Everything here has special data associated
// with it. Otherwise, it just goes in the
// `qiish.rs` file.

/// CAT command (conCATenate)
pub mod cat;
/// CP command (CoPy)
pub mod cp;
/// MV command (MoVe)
pub mod mv;
/// RM command (ReMove)
pub mod rm;
